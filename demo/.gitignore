# The demo is a legible directory account: its records are the tracked
# files, so there is no LMDB store and nothing to export.  Only the
# compiled objects and the cataloged binary are derived — rebuild them
# with BASIC + CATALOG.
BP.O/
BP.O.DICT/
CATALOG/
